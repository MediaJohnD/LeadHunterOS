# LeadHunterOS v2 - Python dependencies
# Python 3.11+
#
# Install: pip install -r requirements.txt
# (No venv required - run from master prompt)

# ── LLM clients ──────────────────────────────────────────────────────────────
# openai client works for Lemonade local (AMD) AND OpenAI cloud
# Lemonade is OpenAI-API compatible - same client, different base_url
openai>=1.30.0
anthropic>=0.25.0              # Claude Pro / Anthropic (US-based, heavy tasks)
# Perplexity uses OpenAI-compatible API - openai client covers it

# ── HTTP ─────────────────────────────────────────────────────────────────────
httpx>=0.27.0
requests>=2.32.0

# ── Config / environment ─────────────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.7.0

# ── Logging ──────────────────────────────────────────────────────────────────
loguru>=0.7.2

# ── Data / parsing ───────────────────────────────────────────────────────────
beautifulsoup4>=4.12.3
lxml>=5.2.1

# ── Database (optional - SQLite works out of box) ────────────────────────────
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
dnspython>=2.6.1
jobspy>=1.1.80

# ── US-based API clients (optional enrichment) ───────────────────────────────
# sendgrid>=6.11.0          # SendGrid / Twilio (US company) - uncomment for email outreach

# ── REMOVED (no longer needed) ───────────────────────────────────────────────
# apscheduler  REMOVED - replaced with stdlib time.sleep() loop in run_agent.py
# rich         REMOVED - replaced with loguru logging
# typer        REMOVED - replaced with stdlib argparse
# tenacity     REMOVED - retry logic handled inline
# aiohttp      REMOVED - httpx covers async needs
