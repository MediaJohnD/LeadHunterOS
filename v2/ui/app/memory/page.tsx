import { StubPage } from "@/components/layout/stub-page";

export default function MemoryPage() {
  return (
    <StubPage
      title="LeadHunterOS Memory"
      description="Inspect trajectory artifacts, entity graph memory, and policy snapshots across executive operations runs."
    />
  );
}

