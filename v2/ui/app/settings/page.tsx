import { StubPage } from "@/components/layout/stub-page";

export default function SettingsPage() {
  return (
    <StubPage
      title="LeadHunterOS Settings"
      description="Set runtime controls, provider failover limits, security defaults, and PWA behavior for LeadHunterOS."
    />
  );
}

